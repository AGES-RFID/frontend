# ==============================================================================
# ⚠️ AVISO DE SEGURANÇA ⚠️
# ==============================================================================
# 1. NÃO COLOQUE SENHAS REAIS, CHAVES DE API OU SEGREDOS DE PRODUÇÃO AQUI.
# 2. Copie este arquivo para um novo arquivo chamado apenas '.env' (cp .env.example .env).
# 3. Preencha o .env com seus valores locais/reais.
# 4. O arquivo '.env' está incluído no .gitignore e NUNCA deve ser commitado.
# ==============================================================================

API_URL=http://localhost:5000/api
